# must be unique in a given SonarQube instance
sonar.projectKey=search-bar-taxScouts

# --- optional properties ---

# defaults to project key
sonar.projectName=search-bar-taxScouts
# defaults to 'not provided'
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to ".". Better if we put src/
sonar.sources=src/
sonar.exclusions=**.test.tsx, **.test.ts, src/assets/placeholder.ts
sonar.coverage.exclusions=src/index.tsx, src/assets/placeholder.ts, src/actions/searchHistory.ts, src/reducers/searchHistory.ts
# Path to test reports (it´s always the same) and expected test file config
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

#----- Default SonarQube server
sonar.host.url=http://localhost:9000
sonar.login= f37bfef11638f1140c8e9af178449798d9127040